## Install Instruction

# open Terminal

# ffmpeg
iex (irm ffmpeg.tc.ht)

# virtual env
pip install virtualenv
python -m venv venv
venv\Scripts\activate

# dependency
pip install -r .\requirements.txt    